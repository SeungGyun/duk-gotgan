"""무엇을 담을지 — 한 곳에서만 정합니다.

**AI 는 더 이상 탈락시키지 않습니다.** 찾아서 요약까지 한 것은 전부
보여 주고, 버릴지는 사람이 정합니다(제외 버튼).

왜 바꿨는가. 점수 하한만 쓰던 때의 실측(검토 20건)에서 이미 드러났습니다 —
AI 탈락 최고점 56, 통과 최저점 74 로 그 사이 18점이 통째로 비어 있었고,
하한을 75 → 65 로 내려도 추가 통과가 0편이었습니다. 판정으로 바꿔 봤지만
이번엔 `introductory` 를 무엇으로 볼지가 사람마다 다릅니다. **기계가 대신
정할 수 있는 경계가 아니었습니다.**

판정과 점수는 **버리지 않고 보여 줍니다.** 목록에서 무엇을 제외할지
빠르게 고르는 데 쓰는 근거이지, 자동으로 버리는 기준이 아닙니다.
"""

# 요약이 나왔으면 담습니다. 이것 말고 다른 조건은 없습니다.
#
# 요약이 없는 경우는 두 가지뿐입니다 — 모델이 형식을 어겼거나(재시도로
# 처리) 자막이 요약할 수 없는 상태이거나. 둘 다 "판정해서 버린 것"이
# 아니라 **실패**라서 FAILED_REVIEW 로 갑니다.


def should_publish(summary_present: bool) -> bool:
    return summary_present
