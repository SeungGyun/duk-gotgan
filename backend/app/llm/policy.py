"""무엇을 담고 무엇을 버릴지 — 한 곳에서만 정합니다.

**두 곳에서 쓰입니다.** 프롬프트에 넘기는 `summary_threshold`(workspace)와
실제 공개 판정(tools)이 같은 값을 봐야 합니다. 어긋나면 AI 는 "기준 미달"로
보고 요약을 생략했는데 우리 쪽은 통과로 처리해, 요약 없는 덕질이 생깁니다.

**점수가 아니라 판정으로 거릅니다.**

점수 하한만 쓰던 때의 실측(2026-08-01, 검토 20건):

  AI 탈락 최고점  56점  ┐  이 사이에 아무것도 없습니다
  통과 최저점     74점  ┘

하한을 75 → 65 로 내려도 추가로 통과하는 영상이 **0편**이었습니다. 점수는
손잡이 노릇을 못 합니다. 실제로 갈리는 것은 판정입니다 — 홍보물(코인 플랫폼
소개 17점)과 주제 무관은 점수가 몇 점이든 담을 이유가 없고, 개론·실무는
"전문가 강의가 아니어도 볼 만한" 쪽이라 낮은 하한만 넘으면 담습니다.
"""

# 점수와 무관하게 담지 않습니다.
BLOCKED_VERDICTS = frozenset({"promotional", "irrelevant"})

# 판정으로 거르므로, 점수는 "내용이 있는가" 정도만 봅니다.
DEFAULT_THRESHOLD = 45
